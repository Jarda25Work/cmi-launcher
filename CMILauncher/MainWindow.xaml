<Window x:Class="CMILauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="ČMI Launcher" 
        Height="800" Width="800"
        WindowStartupLocation="CenterScreen"
    MinHeight="600" MinWidth="600"
    PreviewKeyDown="Window_PreviewKeyDown">
    
    <Grid>
        <!-- WebView2 zobrazující celou aplikaci launcher.cmi.cz/app -->
        <!-- Source je odstraněn - navigace se řídí z kódu po inicializaci CoreWebView2 -->
        <wv2:WebView2 x:Name="webView" 
                      DefaultBackgroundColor="Transparent" 
                      Visibility="Visible"
                      Panel.ZIndex="1" />

        <!-- Úvodní informační obrazovka -->
        <Grid x:Name="WelcomeScreen"
              Background="White"
              Visibility="Visible"
              Panel.ZIndex="100">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <Border Grid.Row="1" 
                    Background="White" 
                    Padding="40" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center"
                    MaxWidth="600"
                    x:Name="WelcomeBorder">
                <StackPanel x:Name="WelcomeStackPanel">
                    <!-- Logo/Ikona -->
                    <Image Source="Resources/icon.ico" 
                           Width="80" 
                           Height="80" 
                           Margin="0,0,0,24" 
                           HorizontalAlignment="Center"
                           RenderOptions.BitmapScalingMode="HighQuality" />
                    
                    <!-- Název aplikace -->
                    <TextBlock Text="ČMI Launcher"
                               FontSize="28"
                               FontWeight="Bold"
                               Foreground="#333"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,16"/>
                    
                    <!-- Hlavní informace -->
                    <TextBlock x:Name="WelcomeMessage"
                               Text="Připojuji se k aplikačnímu portálu..."
                               FontSize="16"
                               Foreground="#666"
                               TextAlignment="Center"
                               Margin="0,0,0,24"/>
                    
                    <!-- Informační box o certifikátech -->
                    <Border Background="#E3F2FD" 
                            BorderBrush="#2196F3" 
                            BorderThickness="1" 
                            CornerRadius="4" 
                            Padding="16" 
                            Margin="0,0,0,24">
                        <StackPanel>
                            <TextBlock Text="💡 Informace o přihlášení"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Foreground="#1976D2"
                                       Margin="0,0,0,8"/>
                            <TextBlock Text="Při spuštění budete požádáni o zadání PIN kódu k vašemu certifikátu. Toto je bezpečnostní opatření pro ověření vaší identity."
                                       FontSize="12"
                                       Foreground="#424242"
                                       TextWrapping="Wrap"
                                       LineHeight="16"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Progress bar -->
                    <Grid>
                        <Rectangle x:Name="ProgressBackground" 
                                   Height="4" 
                                   Fill="#E0E0E0" 
                                   RadiusX="2" 
                                   RadiusY="2"/>
                        <Rectangle x:Name="ProgressBar" 
                                   Height="4" 
                                   Fill="#2196F3" 
                                   RadiusX="2" 
                                   RadiusY="2"
                                   HorizontalAlignment="Left"
                                   Width="0"/>
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Overlay pro výpadky připojení -->
        <Grid x:Name="RetryOverlay"
              Visibility="Collapsed"
              Panel.ZIndex="500"
              IsHitTestVisible="True">
            <!-- Background overlay to darken screen - does not capture clicks -->
            <Rectangle Fill="#80000000" 
                       IsHitTestVisible="False" />
            
            <!-- Dialog content with hit test enabled -->
            <Border Background="White" 
                    CornerRadius="10" 
                    Padding="30" 
                    MaxWidth="500"
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center"
                    IsHitTestVisible="True">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Direction="315" ShadowDepth="5" Opacity="0.3"/>
                </Border.Effect>
                <StackPanel>
                    <!-- Header -->
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Icon -->
                        <Grid Width="50" Height="50" Margin="0,0,15,0">
                            <Ellipse Fill="#F44336" />
                            <TextBlock Text="⚠" 
                                       FontSize="28" 
                                       Foreground="White" 
                                       HorizontalAlignment="Center" 
                                       VerticalAlignment="Center"/>
                        </Grid>
                        
                        <!-- Title -->
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Chyba připojení"
                                       FontSize="20"
                                       FontWeight="Bold"
                                       Foreground="#333"
                                       Margin="0,0,0,5"/>
                            <TextBlock Text="Nelze se připojit k serveru"
                                       FontSize="13"
                                       Foreground="#666"/>
                        </StackPanel>
                    </Grid>
                    
                    <!-- Message -->
                    <TextBlock x:Name="RetryMessage"
                               Text="Zkontrolujte prosím síťové připojení."
                               FontSize="14"
                               Foreground="#444"
                               TextWrapping="Wrap"
                               Margin="0,0,0,24"
                               TextAlignment="Left"/>
                    
                    <!-- Button -->
                    <Button x:Name="RetryButton" 
                            Click="RetryButton_Click"
                            HorizontalAlignment="Center"
                            Padding="20,10"
                            MinWidth="150">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#F44336"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="FontSize" Value="14"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="5"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" 
                                                                  VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#E53935"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter Property="Background" Value="#C62828"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <TextBlock Text="Zkusit znovu" />
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
        
        <!-- Custom Certificate Dialog -->
        <Grid x:Name="CertificateDialog"
              Visibility="Collapsed"
              Panel.ZIndex="1000"
              IsHitTestVisible="True">
            <!-- Invisible background overlay to darken screen -->
            <Rectangle Fill="#80000000" 
                       IsHitTestVisible="False" />
            
            <!-- Dialog content with hit test enabled -->
            <Border Background="White" 
                    CornerRadius="10" 
                    Padding="30" 
                    MaxWidth="500"
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center"
                    IsHitTestVisible="True">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Direction="315" ShadowDepth="5" Opacity="0.3"/>
                </Border.Effect>
                <StackPanel>
                    <!-- Header -->
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Icon -->
                        <Grid Width="50" Height="50" Margin="0,0,15,0">
                            <Ellipse Fill="#2196F3" />
                            <TextBlock Text="🔒" 
                                       FontSize="24" 
                                       Foreground="White" 
                                       HorizontalAlignment="Center" 
                                       VerticalAlignment="Center"/>
                        </Grid>
                        
                        <!-- Title -->
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Výběr způsobu přihlášení" 
                                       FontSize="18" 
                                       FontWeight="Bold" 
                                       Foreground="#333"/>
                            <TextBlock x:Name="CertDialogSubtitle"
                                       Text="Nalezen komerční certifikát pro přihlášení" 
                                       FontSize="12" 
                                       Foreground="#666" 
                                       Margin="0,3,0,0"/>
                        </StackPanel>
                    </Grid>
                    
                    <!-- Certificate Info -->
                    <TextBlock x:Name="CertDialogMessage"
                               Text="Nalezený certifikát: [Certificate Name]"
                               FontSize="13"
                               Foreground="#333"
                               Margin="0,0,0,20"
                               TextWrapping="Wrap"/>
                    
                    <!-- Instructions -->
                    <TextBlock Text="Chcete pokračovat s certifikátem nebo se přihlásit bez něj?"
                               FontSize="13"
                               Foreground="#555"
                               Margin="0,0,0,20"
                               TextWrapping="Wrap"/>
                    
                    <!-- Buttons -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Use Certificate Button -->
                        <Button x:Name="UseCertificateButton"
                                Grid.Column="0"
                                Content="Použít certifikát"
                                Background="#2196F3"
                                Foreground="White"
                                FontWeight="Bold"
                                Padding="10,8"
                                BorderThickness="0"
                                Click="UseCertificateButton_Click"
                                MouseDown="UseCertificateButton_MouseDown"
                                IsEnabled="True"
                                Focusable="True"
                                IsHitTestVisible="True"/>
                        
                        <!-- Continue Without Certificate Button -->
                        <Button x:Name="ContinueWithoutCertButton"
                                Grid.Column="2"
                                Content="Bez certifikátu"
                                Background="#FFC107"
                                Foreground="White"
                                FontWeight="Bold"
                                Padding="10,8"
                                BorderThickness="0"
                                Click="ContinueWithoutCertButton_Click"
                                MouseDown="ContinueWithoutCertButton_MouseDown"
                                IsEnabled="True"
                                Focusable="True"
                                IsHitTestVisible="True"/>
                        
                        <!-- Cancel Button -->
                        <Button x:Name="CancelCertButton"
                                Grid.Column="4"
                                Content="Zrušit"
                                Background="#F44336"
                                Foreground="White"
                                FontWeight="Bold"
                                Padding="10,8"
                                BorderThickness="0"
                                Click="CancelCertButton_Click"
                                MouseDown="CancelCertButton_MouseDown"
                                IsEnabled="True"
                                Focusable="True"
                                IsHitTestVisible="True"/>
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
